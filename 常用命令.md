uv run train Mjlab-Velocity-Flat-Unitree-G1 --env.scene.num-envs 4096

uv run train Mjlab-Velocity-Flat-Forward-Custom-Biped --env.scene.num-envs 4096

uv run train Mjlab-Velocity-Flat-Forward-Custom-Biped-NoLinVel --env.scene.num-envs 256

uv run play Mjlab-Velocity-Flat-Unitree-G1 --wandb-run-path chen-xxxiii4399-beijing-institute-of-technology/mjlab/q9si0uhr

uv run play Mjlab-Velocity-Flat-Forward-Custom-Biped --wandb-run-path chen-xxxiii4399-beijing-institute-of-technology/mjlab/wp390p0g

uv run play Mjlab-Velocity-Flat-Forward-Custom-Biped-NoLinVel --wandb-run-path chen-xxxiii4399-beijing-institute-of-technology/mjlab/wp390p0g

uv run play Mjlab-Velocity-Flat-Forward-Custom-Biped --checkpoint-file 'logs/rsl_rl/custom_biped_velocity/2026-05-23_02-19-59/model_29999.pt'

uv run play Mjlab-Velocity-Flat-Forward-Custom-Biped-NoLinVel --checkpoint-file 'logs/rsl_rl/custom_biped_velocity_nolinvel/2026-07-20_16-18-45_real_motor_phase_effort_rebalance/model_54494.pt'




## 三个项目的电机 PD 参数

三个项目使用同一套物理 PD 参数，但由于 FDCAN 接口和仿真的单位不同，
代码中的数值分为“接口调用值”“电机内部值”和“弧度制仿真值”。

### 1. robot 固件

文件：`/home/cx/robot/Core/Src/main.c`

```c
#define USB_CONTROL_KP_NM_PER_RAD       0.1589384924f
#define USB_CONTROL_KD_NMS_PER_RAD      0.1589384924f
#define USB_CONTROL_KP_NM_PER_TURN      1.0f
#define USB_CONTROL_KD_NMS_PER_TURN     1.0f
```

真正传入 `motor_many_pos_vel_tqe_kp_kd_2()` 的是每圈参数：

```text
Kp_api = 1.0
Kd_api = 1.0
```

六个电机使用相同的 Kp/Kd。当前目标速度和前馈力矩均为 0：

```text
v_des = 0
torque_ff = 0
```

M4438_30 的力矩修正系数为：

```text
k_motor = 0.5256
```

SDK 首先进行型号补偿：

```text
Mkp_unquantized = Kp_api / k_motor
                = 1.0 / 0.5256
                = 1.902587519

Mkd_unquantized = Kd_api / k_motor
                = 1.0 / 0.5256
                = 1.902587519
```

一拖多控制使用 int16 协议，Kp/Kd 的编码比例为 10：

```text
kp_code = int16(Mkp_unquantized * 10) = 19
kd_code = int16(Mkd_unquantized * 10) = 19
```

所以电机实际收到的内部系数为：

```text
Mkp_sent = kp_code / 10 = 1.9
Mkd_sent = kd_code / 10 = 1.9
```

电机内部按圈和圈/秒执行 PD：

```text
torque_internal =
    Mkp_sent * (q_des_turn - q_turn)
  + Mkd_sent * (v_des_turn_s - v_turn_s)
  + torque_ff_internal
```

换算成输出侧物理力矩，可写成：

```text
torque_Nm =
    k_motor * Mkp_sent * (q_des_turn - q_turn)
  + k_motor * Mkd_sent * (v_des_turn_s - v_turn_s)
  + torque_ff_Nm
```

### 2. mjlab-recovered 训练仿真

文件：
`/home/cx/mjlab-recovered/src/mjlab/asset_zoo/robots/custom_biped/biped_constants.py`

```python
HTDW4438_POSITION_STIFFNESS = 0.15893849236929034
HTDW4438_POSITION_DAMPING = 0.15893849236929034
```

mjlab 使用 rad、rad/s 和 N·m，因此需要将电机实际收到的内部系数换算为
输出侧弧度制增益：

```text
Kp_rad = k_motor * Mkp_sent / (2*pi)
       = 0.5256 * 1.9 / (2*pi)
       = 0.15893849236929034 N*m/rad

Kd_rad = k_motor * Mkd_sent / (2*pi)
       = 0.5256 * 1.9 / (2*pi)
       = 0.15893849236929034 N*m*s/rad
```

mjlab 中的 PD 公式为：

```text
torque_Nm =
    Kp_rad * (q_des_rad - q_rad)
  + Kd_rad * (v_des_rad_s - v_rad_s)
```

当前 `v_des=0`，因此也可以写成：

```text
torque_Nm =
    0.15893849236929034 * (q_des_rad - q_rad)
  - 0.15893849236929034 * v_rad_s
```

### 3. robot_deploy 独立 MuJoCo 仿真

文件：`/home/cx/robot_deploy/pc_32_mujoco.py`

```python
MOTOR_KP = 0.15893849236929034
MOTOR_KD = 0.15893849236929034
```

PD 计算公式为：

```python
desired = MOTOR_KP * (target - position) - MOTOR_KD * velocity
```

这里的 `target`、`position` 和 `velocity` 分别使用 rad、rad 和 rad/s，
`desired` 的单位为 N·m。计算结果随后还会受到 2 N·m 连续力矩限制以及
电机力矩-速度曲线的限制。

### 参数对应关系

| 层级 | Kp | Kd | 单位/含义 |
|---|---:|---:|---|
| robot FDCAN API 调用值 | 1.0 | 1.0 | N·m/turn、N·m·s/turn |
| 补偿后未量化内部值 | 1.902587519 | 1.902587519 | 电机内部系数 |
| FDCAN int16 编码 | 19 | 19 | 报文整数值 |
| 电机实际收到的值 | 1.9 | 1.9 | 电机内部系数 |
| mjlab-recovered | 0.15893849236929034 | 0.15893849236929034 | N·m/rad、N·m·s/rad |
| robot_deploy MuJoCo | 0.15893849236929034 | 0.15893849236929034 | N·m/rad、N·m·s/rad |

注意：现有 ONNX 如果是在旧 Kp/Kd 下训练得到的，修改这些参数后需要重新训练
并重新导出 ONNX，不能仅修改固件后继续假设旧模型与新电机动力学匹配。

## MuJoCo viewer 快捷键

- Enter: reset 环境
- Space: 暂停/继续
- 右方向键: 暂停时单步前进
- C: 显示/隐藏 contact points
- S: 显示/隐藏 sites
- R: 显示/隐藏 mjlab debug visualization
- P: 显示/隐藏 reward plots
- A: 显示/隐藏所有 envs''
- , / .: 切换上一个/下一个 env
- - / =: 降低/提高播放速度

##速度指令
uv run play Mjlab-Velocity-Flat-Forward-Custom-Biped-NoLinVel --wandb-run-path chen-xxxiii4399-beijing-institute-of-technology/mjlab/lqt9q2tx --command-x 0.2
